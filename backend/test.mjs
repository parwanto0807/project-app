import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); prisma.sPK.findMany({where: {spkFieldReport: {some: {}}}, take: 5, orderBy: {updatedAt: 'desc'}, include: {spkFieldReport: {include: {photos: true}}}}).then(res => { console.log(res.map(s => s.spkFieldReport[0].photos.length)); prisma.$disconnect(); }).catch(e => { console.error(e); prisma.$disconnect(); })
