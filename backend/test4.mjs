import fs from 'fs'; import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); prisma.sPKFieldReportPhoto.findMany().then(res => { const dbFiles = res.map(p => p.imageUrl.split('/').pop()); const diskFiles = fs.readdirSync('./public/images/spk'); const common = dbFiles.filter(f => diskFiles.includes(f)); console.log('Common files:', common.length > 0 ? common : 'NONE'); prisma.$disconnect(); }).catch(e => { console.error(e); prisma.$disconnect(); })
