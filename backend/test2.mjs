import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); prisma.sPK.findMany({where: {spkFieldReport: {some: {}}}, take: 1, orderBy: {updatedAt: 'desc'}, include: {spkFieldReport: {include: {photos: true}}}}).then(res => { console.log(res[0].spkFieldReport[0].photos[0].imageUrl); prisma.$disconnect(); }).catch(e => { console.error(e); prisma.$disconnect(); })
