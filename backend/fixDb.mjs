import fs from 'fs'; import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function run() { const diskFiles = fs.readdirSync('./public/images/spk').filter(f => f.endsWith('.jpg') || f.endsWith('.png')); if(diskFiles.length === 0) return; const testFile = '/images/spk/' + diskFiles[0]; const latestPhotos = await prisma.sPKFieldReportPhoto.findMany({take: 5, orderBy: {createdAt: 'desc'}}); for(const p of latestPhotos) { await prisma.sPKFieldReportPhoto.update({where: {id: p.id}, data: {imageUrl: testFile}}); } console.log('Updated', latestPhotos.length, 'photos to use', testFile); await prisma.$disconnect(); } run().catch(console.error);
